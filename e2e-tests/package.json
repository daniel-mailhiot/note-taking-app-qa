{
  "name": "notes-app-e2e-tests",
  "version": "1.0.0",
  "description": "Playwright runner for the Notes App UI smoke suite.",
  "private": true,
  "scripts": {
    "smoke": "playwright test --grep @smoke"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.8.0"
  }
}